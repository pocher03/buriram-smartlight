# ============================================================
# .env.example — ศูนย์บัญชาการโคมไฟถนนอัจฉริยะ เทศบาลเมืองบุรีรัมย์
# คัดลอกเป็น .env.local แล้วเติมค่าจริง (.env.local ถูก gitignore)
# 🔴 ห้ามใช้ NEXT_PUBLIC_ กับค่าลับ — ค่าลับทั้งหมดอยู่ฝั่ง Worker/Backend เท่านั้น
# ============================================================

# ---- Data source (mock | live) — สลับ Adapter ----
# Sprint 2+: ใช้ live (Frontend อ่านจาก Local DB ที่ Worker เติมให้)
DATA_SOURCE=live

# ---- RULR AIoT (ต้นทาง) — Worker/Backend เท่านั้น (Sprint 2) ----
RULR_BASE_URL=https://rulr-aiot.com
RULR_CLIENT_ID=
RULR_CLIENT_SECRET=
RULR_DIVISION_ID=1889
RULR_DOMAIN=IMPACT

# ---- OpenWeatherMap (สภาพอากาศเทศบาล) — Worker เท่านั้น (Sprint 2) ----
OPENWEATHER_API_KEY=
WEATHER_LAT=14.992892
WEATHER_LNG=103.113694

# ---- PostgreSQL (Prisma + docker-compose) ----
# ⚠️ ภาพจริงบน Droplet: ไม่ได้ตั้ง POSTGRES_USER/POSTGRES_DB → container ใช้ค่า default
#    ของ image = user "postgres" / db "postgres" ดังนั้น DATABASE_URL ต้องตรงกับ default นี้
#    ถ้าจะเปลี่ยนเป็น user/db เฉพาะ ต้องตั้ง 3 ค่าให้ตรงกันทั้งหมด (USER/DB/DATABASE_URL)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=postgres
# ภายใน docker network ใช้ host = "postgres"; dev บนเครื่องใช้ localhost
DATABASE_URL=postgresql://postgres:CHANGEME@postgres:5432/postgres?schema=public

# ---- Auth.js (NextAuth v5) — Sprint 3 ----
# AUTH_SECRET: สร้างด้วย `npx auth secret` หรือ `openssl rand -base64 32` (ห้ามว่าง บน production)
AUTH_SECRET=
AUTH_URL=https://monitor.jumboelec.co.th

# ---- Seed users (Sprint 3) — ถ้าไม่ตั้ง จะใช้ค่าเริ่มต้นใน prisma/seed.ts ----
# ค่าเริ่มต้น: admin_buriram/Buriram@2569 · jumbo_admin/Jumbo@2569 · jumbo_backup/Backup@2569
# แนะนำให้ตั้งรหัสผ่านจริงที่นี่ก่อนรัน `npm run db:seed` บน production
SEED_ADMIN_USER=
SEED_ADMIN_PASS=
SEED_ADMIN_EMAIL=
SEED_SUPER1_USER=
SEED_SUPER1_PASS=
SEED_SUPER1_EMAIL=
SEED_SUPER2_USER=
SEED_SUPER2_PASS=
SEED_SUPER2_EMAIL=

# ---- Email (Resend) — ลืมรหัสผ่าน, เลื่อนไป Sprint 4 ----
RESEND_API_KEY=

# ---- Deploy / Caddy ----
APP_DOMAIN=monitor.jumboelec.co.th
# อีเมลสำหรับออก Let's Encrypt cert ผ่าน Caddy
CADDY_ACME_EMAIL=ops@jumboelec.co.th
